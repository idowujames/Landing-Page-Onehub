import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Onehub - Smart Energy Monitoring for Nigerian Homes",
  description:
    "Real-time visibility into your electricity consumption. Track every naira, understand your usage, and take control of your energy costs with Onehub.",
  keywords: ["energy monitoring", "smart home", "Nigeria", "electricity", "power tracking", "energy savings"],
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
